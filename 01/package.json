{
  "dependencies": {
    "commander": "^7.2.0"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "@types/node": "^15.0.0",
    "trash-cli": "^4.0.0",
    "ts-node": "^9.1.0",
    "typescript": "^4.2.0"
  },
  "engines": {
    "node": "^14.0.0",
    "npm": "^6.0.0"
  },
  "name": "caesar-cipher-cli-tool",
  "private": true,
  "scripts": {
    "build": "tsc --outDir ./сaesar-cipher-cli --removeComments",
    "clear": "trash ./сaesar-cipher-cli",
    "postinstall": "npm run build"
  }
}
